# Ascension-Custom-Addons

Repo-wide conventions live in `management/docs/CLAUDE.md` and are imported below. This root
file exists only so that import happens: Claude Code auto-loads `CLAUDE.md` from the working
directory and its parents, so a file kept solely in `management/docs/` would be skipped for
sessions started at the repo root.

@management/docs/CLAUDE.md
